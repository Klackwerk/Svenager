<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>false</withJansi>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${CONSOLE_LOG_THRESHOLD}</level>
        </filter>
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>${CONSOLE_LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <!-- Persistent log for audit/ops review; path via LOG_FILE/LOG_PATH. -->
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH:-logs}/svenager.log}"/>
    <include resource="org/springframework/boot/logging/logback/file-appender.xml"/>

    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="de.klackwerk.svenager" level="INFO"/>

    <springProfile name="development">
        <logger name="StackTrace" level="ERROR" />

<!--        <logger name="Test38" level="DEBUG"/>-->

<!--        <logger name="org.hibernate.SQL" level="DEBUG"/>-->
<!--        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>-->
<!--        <logger name="org.springframework.security" level="TRACE"/>-->

<!--        <root level="WARN"/>-->
    </springProfile>

</configuration>
