---
- name: Converge
  hosts: all
  become: true
  vars:
    # Injected by the Svenager agent on real devices.
    svenager_server_url: https://svenager.example.org
    motd_banner_extra: Molecule test line
  roles:
    - svenager_base
    - motd_banner
